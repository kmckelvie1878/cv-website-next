import { Global } from '@emotion/react'

const Fonts = () => (
    <Global
        styles={`
            @import url("https://fonts.googleapis.com/css2?family=M+PLUS+2:wght@300;400;700;900&display=swap");
        `}
    />
)

export default Fonts